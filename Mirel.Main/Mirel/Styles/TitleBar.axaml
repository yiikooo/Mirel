<Styles xmlns="https://github.com/avaloniaui" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Design.PreviewWith>
        <Border Padding="20">
            <!--  Add Controls for Previewer Here  -->
        </Border>
    </Design.PreviewWith>

    <!--  Add Styles Here  -->
    <Style Selector="Button.title-bar-button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="25" />
        <Setter Property="Width" Value="25" />
        <Setter Property="Template">
            <ControlTemplate>
                <Border
                    Background="{TemplateBinding Background}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="7"
                    Height="24"
                    Name="bd"
                    Width="24">
                    <Path
                        Data="{TemplateBinding Content}"
                        Fill="{DynamicResource TextColor}"
                        Height="10"
                        HorizontalAlignment="Center"
                        Margin="{TemplateBinding Padding}"
                        Name="ico"
                        Stretch="Uniform"
                        VerticalAlignment="Center"
                        Width="10" />
                </Border>
            </ControlTemplate>
        </Setter>
        <Style Selector="^:pointerover">
            <Style Selector="^ /template/ Border">
                <Setter Property="Background" Value="{DynamicResource TitleBarHoverColor}" />
            </Style>
            <Style Selector="^.close-button /template/ Border">
                <Setter Property="Background" Value="Red" />
            </Style>
            <Style Selector="^.close-button /template/ Border">
                <Style Selector="^ Path">
                    <Setter Property="Fill" Value="White" />
                </Style>
            </Style>
        </Style>
    </Style>
    <Style Selector="Button.custom-hover-button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="25" />
        <Setter Property="Width" Value="25" />
        <Setter Property="Template">
            <ControlTemplate>
                <Border
                    Background="{TemplateBinding Background}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="7"
                    Height="24"
                    Name="bd"
                    Width="24">
                    <Path
                        Data="{TemplateBinding Content}"
                        Fill="{DynamicResource TextColor}"
                        Height="20"
                        Name="ico"
                        Stretch="Uniform"
                        Width="20" />
                </Border>
            </ControlTemplate>
        </Setter>
        <Style Selector="^:pointerover">
            <Style Selector="^ /template/ Border">
                <Setter Property="Background" Value="{DynamicResource TitleBarHoverColor}" />
            </Style>
        </Style>
    </Style>
    <Style Selector="Button.icon-button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="25" />
        <Setter Property="Width" Value="25" />
        <Setter Property="Template">
            <ControlTemplate>
                <Border
                    Background="{TemplateBinding Background}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="{DynamicResource MainCornerRadius}"
                    Name="bd">
                    <Path
                        Data="{TemplateBinding Content}"
                        Fill="{DynamicResource TextColor}"
                        Height="9"
                        Name="ico"
                        Stretch="Uniform"
                        Width="9" />
                </Border>
            </ControlTemplate>
        </Setter>
    </Style>
    <Style Selector="Button.big-title-bar-icon">
        <Style Selector="^ Path">
            <Setter Property="Width" Value="14" />
            <Setter Property="Height" Value="14" />
        </Style>
    </Style>
    <Style Selector="Button.thin-title-bar-icon">
        <Style Selector="^ Path">
            <Setter Property="Width" Value="4" />
        </Style>
    </Style>
    <Style Selector="Button.title-bar-button-more">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="25" />
        <Setter Property="Width" Value="25" />
        <Setter Property="Template">
            <ControlTemplate>
                <Border
                    Background="{TemplateBinding Background}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="7"
                    Height="24"
                    Name="bd"
                    Width="24">
                    <Path
                        Data="{TemplateBinding Content}"
                        Fill="{DynamicResource TextColor}"
                        Height="14"
                        HorizontalAlignment="Center"
                        Margin="{TemplateBinding Padding}"
                        Name="ico"
                        Stretch="Uniform"
                        VerticalAlignment="Center"
                        Width="4" />
                </Border>
            </ControlTemplate>
        </Setter>
        <Style Selector="^:pointerover">
            <Style Selector="^ /template/ Border">
                <Setter Property="Background" Value="{DynamicResource TitleBarHoverColor}" />
            </Style>
        </Style>
    </Style>

    <Style Selector="Button.list-item-button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="15" />
        <Setter Property="Width" Value="15" />
        <Setter Property="Template">
            <ControlTemplate>
                <Border
                    Background="{TemplateBinding Background}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="{DynamicResource MainCornerRadius}"
                    Name="bd">
                    <Path
                        Data="{TemplateBinding Content}"
                        Fill="{DynamicResource TextColor}"
                        Height="{TemplateBinding Height}"
                        Name="ico"
                        Stretch="Uniform"
                        Width="{TemplateBinding Width}" />
                </Border>
            </ControlTemplate>
        </Setter>
    </Style>
    <Style Selector="Button.custom-button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="15" />
        <Setter Property="Width" Value="15" />
        <Setter Property="Template">
            <ControlTemplate>
                <Border
                    Background="{TemplateBinding Background}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="{DynamicResource MainCornerRadius}"
                    Name="bd">
                    <Path
                        Data="{TemplateBinding Content}"
                        Fill="{DynamicResource TextColor}"
                        Height="{TemplateBinding Height}"
                        Name="ico"
                        Stretch="Uniform"
                        Width="{TemplateBinding Width}" />
                </Border>
            </ControlTemplate>
        </Setter>
    </Style>
    <Style Selector="Button.tab /template/ Path">
        <Setter Property="Fill" Value="{DynamicResource TextColor}" />
    </Style>
</Styles>